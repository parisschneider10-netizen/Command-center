# Launch sequence — autopilot armed

> **Status:** ARMED · Money in → firewall + closers fire instantly · Commander states intent once

## Autopilot (ON by default)

| Switch | Value |
|--------|--------|
| `EMPIRE_MAX_VELOCITY` | true |
| `INTENT_DEFAULT_AUTO_EXECUTE` | true |
| `EMPIRE_LAUNCH_MODE` | true |
| `INTENT_AUTO_POST_RAH` | true |
| `TREASURY_SANDBOX_INSTANT_CLEAR` | true |

## Voice line

**+1 (971) 382-0038** → SARA → Servury Command Center (not Replit)

**Launch phrase:**
> *"Sovereign stay MTR leads. Max speed. Auto execute."*

## Human firewall (ready when money lands)

1. **Digital block** → A2A/agent queue first
2. **Physical/judgment** → `escalate_to_human` → guardian slot OR RentAHuman bounty
3. **Commander** → nuclear queue only (legal, big money, public brand)

Empty guardian slots → auto-post RentAHuman on judgment walls. No waiting for Commander motivation.

## Revenue first (parallel)

- Sovereign Stay presale ($150 door, $30 closer)
- MTR lead scrape → easiest planetary nodes first
- Treasury compound same-day → next wave

## Big-tech exit queue

1. Replit Overwatch → this repo (in progress)
2. Vapi → LiveKit when `voice` treasury clears
3. Cursor → self-hosted agents when `compute` treasury clears
